version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install --frozen-lockfile
    build:
      commands:
        - yarn webnext:build
  artifacts:
    baseDirectory: packages/webnext/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
