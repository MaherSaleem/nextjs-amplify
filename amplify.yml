version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands:
        - yarn build
        - echo hi1
        - ls packages
        - ls packages/webnext
        - ls packages/webnext/.next
        - echo hi2
  artifacts:
    baseDirectory: packages/webnext/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
