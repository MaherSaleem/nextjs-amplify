version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - yarn install
        build:
          commands:
            - echo Maher
            - echo $BUILD_ENV
            - echo Mahe2
            - yarn run build
      artifacts:
        baseDirectory: packages/webnext/.next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
      buildPath: /
    appRoot: packages/webnext
